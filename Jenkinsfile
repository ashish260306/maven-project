pipeline {
  agent any
     stages{
	    stage('init'){
		  steps {
		    echo "Testing..."
		  }
		}
		
		stage('Build'){
		  steps {
		    echo "Compile..."
		  }
		}
		
		stage('Deploy'){
		   steps {
		      echo "Deploy..."
			}
		}
	}
}
